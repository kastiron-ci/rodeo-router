# Join Interfaces, Security Groups, and IP Configuration
/meld/{name}: string
/meld/{name}/interfaces/{interface-name}: list[string]
/meld/{name}/ip/{ip-name}: list[string]
/meld/{name}/dhcp/{ip-name}: list[string]
/meld/{name}/security-groups/{security-group-name}: list[string]

# Aliases
aliases:
  meld:
  - join
  - m
